import { StyleSheet } from 'react-native';

export const viewStyles = () => {
  return StyleSheet.create({
    flexBox: {
      flex: 1,
      flexDirection: 'row',
      flexWrap: 'wrap',
    },
  });
};
